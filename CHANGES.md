# 📋 گزارش تغییرات — چی کار شد؟

تاریخ: ۱۴۰۵/۰۶/۰۸ (۳۰ اوت ۲۰۲۶)

به زبان خیلی ساده، سه تا کار انجام شد تا خط لوله‌ی انتشار طرح‌ها درست کار کند:

---

## ۱) اکشن گیت‌هاب روی برنچ اشتباه گوش می‌داد 🎧

**مشکل چی بود؟**
ورک‌فلو طوری تنظیم شده بود که فقط وقتی روی برنچ `main` چیزی پوش می‌کنی اجرا شود. اما برنچ پروژه‌ی تو `master` است. یعنی مثل این بود که زنگ در خانه‌ای دیگر را نصب کرده باشی — پوش می‌کردی ولی اکشن **هرگز اجرا نمی‌شد**.

**چی شد؟**
حالا هر دو برنچ (`main` و `master`) را گوش می‌دهد:

```yaml
branches: [main, master]
```

---

## ۲) مقایسه‌ی کامیت‌ها شکست خفیف داشت 🔍

**مشکل چی بود؟**
اسکریپت برای اینکه بفهمد کدام طرح‌ها عوض شده‌اند، دو کامیت آخر را با هم مقایسه می‌کند. ولی فقط ۲ کامیت آخر دانلود می‌شد. اگر یک‌جا چند کامیت با هم پوش می‌کردی، مقایسه شکست می‌خورد و اسکریپت **بی‌صدا هیچ کاری نمی‌کرد** — بدون هیچ پیام خطایی!

**چی شد؟**
- حالا کل تاریخچه دانلود می‌شود (`fetch-depth: 0`) پس مقایسه همیشه جواب می‌دهد.
- اگر باز هم مقایسه شکست خورد، به جای سکوت، **همه‌ی طرح‌ها بررسی و منتشر می‌شوند** و یک پیام هشدار چاپ می‌شود. (بهتر است همه منتشر شود تا هیچ‌چیز!)

---

## ۳) عکس‌ها بی‌دلیل دوباره آپلود می‌شدند 📤

**مشکل چی بود؟**
اگر فقط متن یا تگ‌های یک طرح را در فایل json عوض می‌کردی (بدون دست زدن به عکس)، باگ قبلی عکس را هم دوباره آپلود می‌کرد — اتلاف وقت و پهنای باند.

**چی شد؟**
حالا فقط اگر **خودِ فایل عکس** عوض شده باشد، عکس دوباره آپلود می‌شود. تغییر متن و تگ‌ها سریع و بدون آپلود مجدد اعمال می‌شود.

---

## ۴) فایل `.gitignore` اضافه شد 🧹

فایل‌های اضافی و خطرناک (مثل کلید API یا فایل‌های موقت ویندوز) به‌طور خودکار از مخزن دور نگه داشته می‌شوند.

---

## ✅ حالا چی کار کنی؟

1. این تغییرات با همین پوش به گیت‌هاب رفت.
2. یک **عکس طرح** (مثلاً `astronaut-cat.png`) داخل پوشه‌ی `designs/cats/` بگذار — فعلاً فقط فایل json هست و بدون عکس چیزی منتشر نمی‌شود.
3. مطمئن شو کلید API را در گیت‌هاب گذاشته‌ای:
   **Settings → Secrets and variables → Actions →** کلیدی به نام `POOSHAI_TOKEN` با مقدار `pshai_...`
4. بعد از پوش، برو تب **Actions** و ببین اکشن سبز شد ✔
